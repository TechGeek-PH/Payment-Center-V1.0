plugins {
    id 'com.android.application'
}

android {
    namespace 'com.techgeekph.chatsupport'
    compileSdk 35

    defaultConfig {
        applicationId 'com.techgeekph.chatsupport'
        minSdk 23
        targetSdk 35
        versionCode 100
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
