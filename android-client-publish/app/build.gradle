plugins {
    id 'com.android.application'
}

android {
    namespace 'com.techgeekph.clientportal'
    compileSdk 35

    defaultConfig {
        applicationId 'com.techgeekph.clientportal'
        minSdk 23
        targetSdk 35
        versionCode 300
        versionName '3.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
